# Xcode
xcuserdata/
DerivedData/
build/
*.xcresult

# Generated third-party artifacts — reproducible via scripts/build-pjsip.sh.
# Never commit opaque prebuilt binaries (CLAUDE.md git workflow).
# (ThirdParty/* not ThirdParty/ — a whole-dir exclude would make the
# README negation below ineffective.)
ThirdParty/*
!ThirdParty/README.md

# TestPBX throwaway TLS material (regenerated by TestPBX/generate-certs.sh)
TestPBX/asterisk/keys/

# Secrets and local-only config
.env
*.p12
*.pem
*.key
*.mobileprovision
*.provisionprofile
CLAUDE.local.md

# Packaging output
dist/

# macOS
.DS_Store
